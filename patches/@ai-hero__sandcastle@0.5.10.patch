diff --git a/dist/Display.js b/dist/Display.js
index de05f314bb32d8276b917bec5f989e6d5742ce9f..9afacb233293026f5a52a6ea05a0f4845f89a4a3 100644
--- a/dist/Display.js
+++ b/dist/Display.js
@@ -57,6 +57,9 @@ export const FileDisplay = {
         const appendToLog = (line) => fs
             .writeFileString(filePath, line + "\n", { flag: "a" })
             .pipe(Effect.ignore);
+        const appendText = (text) => fs
+            .writeFileString(filePath, text, { flag: "a" })
+            .pipe(Effect.ignore);
         return {
             intro: () => Effect.void,
             status: (message, _severity) => appendToLog(message.replace(/^\[[^\]]+\] /, "")),
@@ -88,7 +91,7 @@ export const FileDisplay = {
                 yield* appendToLog(`${title} done (${elapsed}s)`);
                 return result;
             }),
-            text: (message) => appendToLog(message),
+            text: (message) => appendText(message),
             toolCall: (name, formattedArgs) => appendToLog(`${name}(${formattedArgs})`),
         };
     })),
diff --git a/dist/Orchestrator.js b/dist/Orchestrator.js
index 9ebc1e3d68f9502b0bf070c9bde7d9518e579dc6..b3fd77c9b63f0b6e656ed4d1cb8e714cb40b24b0 100644
--- a/dist/Orchestrator.js
+++ b/dist/Orchestrator.js
@@ -213,6 +213,7 @@
                 const { result: agentOutput, sessionId } = yield* invokeAgent(ctx.sandbox, ctx.sandboxRepoDir, fullPrompt, provider, idleTimeoutMs, onText, onToolCall, onIdleWarning, options._idleWarningIntervalMs, iterationResumeSession, options.signal);
                 // Flush any remaining buffered text deltas
                 textBuffer.dispose();
+                yield* display.text("\n");
                 yield* display.status(label("Agent stopped"), "info");
                 // Capture session while sandbox is still alive
                 let sessionFilePath;
